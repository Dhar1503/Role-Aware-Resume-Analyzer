# Real resumes

Drop anonymised real resumes here, one sub-folder per category (e.g. `tech_internship/`),
as `name.pdf` + `name.labels.yaml`. See `../validation/README.md` for the label format.
Remove names, phone numbers, emails and addresses before adding a file.
