[project]
name = "resume-analyzer"
version = "2.0.0"
description = "Role-aware resume strength and ATS analyzer"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
